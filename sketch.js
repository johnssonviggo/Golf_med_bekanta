class Player {
    constructor() {
        this.x = 0;
        this.y = 0;
    }
}

let player = new Player();

function setup() {
    createCanvas(400, 400);
}

function draw() {
    background(0);
}